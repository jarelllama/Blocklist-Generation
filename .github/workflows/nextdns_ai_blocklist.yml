name: Build
#run-name:
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
    
      - name: Install and configure NextDNS
        run: |
          curl ${{ secrets.NEXTDNS_LINK_IP }}
          #bash install_nextdns.sh ${{ secrets.NEXTDNS_PROFILE_ID }}
          
      - run: |
          dig @${{ vars.NEXTDNS_DNS1 }} gookjsfkdjfgle.com
