name: Test 3
on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:    
      - run: |
          sudo sh -c 'sh -c "$(curl -sL https://api.controld.com/dl)" -s 2c7h7bby6gb forced'
          dig reddit.com
